<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/main"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/uuid"
        android:layout_margin="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_width="0dp"
        android:layout_height="wrap_content" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/androidid"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/uuid"
        app:layout_constraintRight_toRightOf="@id/uuid"
        app:layout_constraintLeft_toLeftOf="@id/uuid"
        android:layout_width="0dp"
        android:layout_height="wrap_content" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/hardware"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/androidid"
        app:layout_constraintRight_toRightOf="@id/uuid"
        app:layout_constraintLeft_toLeftOf="@id/uuid"
        android:layout_width="0dp"
        android:layout_height="wrap_content" />

</androidx.constraintlayout.widget.ConstraintLayout>